(ns parapepoid.core-test
  (:require [clojure.test :refer :all]
            [parapepoid.core :refer :all]))

(deftest a-test
  (testing "parapepoid.core has no interesting stuff in it, yet."
    (is (= 1 1))))
